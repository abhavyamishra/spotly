# MongoDB connection string. Use MongoDB Atlas URI for production.
DATABASE_URL="mongodb+srv://<user>:<password>@redispro.sf0rtcb.mongodb.net/spotly?retryWrites=true&w=majority"

# Redis connection string
REDIS_URL="redis://localhost:6379"

# JWT secret for signing auth tokens
JWT_SECRET="change-this-to-a-random-secret"

# Cookie domain and secure settings for production
COOKIE_DOMAIN="localhost"
COOKIE_SECURE=false

# SMTP config for real email OTP delivery
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
